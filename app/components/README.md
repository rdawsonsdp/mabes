# Section components live here.
One file per page section (Header.tsx, Hero.tsx, Footer.tsx, ...),
composed in app/page.tsx. Style with Tailwind utilities bound to the
@theme tokens in app/globals.css.
